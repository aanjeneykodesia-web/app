buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.1' // ✅ specify plugin version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
